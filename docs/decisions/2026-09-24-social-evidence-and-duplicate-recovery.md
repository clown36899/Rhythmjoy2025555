# 2026-09-24 — 소셜 원문 근거와 미등록 후보 중복의 경계

## 관측과 변경 경계

사보이 9/24 공지(카페 14933600, 글 102764)는 9/22 11:31 게시됐다. 9/22의 정상 후보 `7b7cc6de0c02240d`는 해림·9/24·사보이볼룸을 검증했지만 duplicate로 저장됐고 공개 일정은 정규 placeholder뿐이었다. 9/24 10:30·11:30에도 실제 실행됐으므로 전원 문제나 재시도 선정 중단이 원인은 아니다.

`원문 DOM → 날짜/DJ 추출 → 후보 동일성 판정 → duplicate terminal skip → 공개 미등록` 경로를 조사했다. 지난주 성공 원문 102745·후보 `d98608881745bf55`·공개 이벤트 `c6d91fe4-bcc3-47d7-bc50-597234de1eca`와 비교했다. 차단한 후보 `512deb57259d153b`는 동일한 9/17 글을 9/24로 추출한 미등록 행이다. 실제 저장 포스터에는 DJ 몽룡이 있고 9/24 날짜는 없지만 AI 전사에는 9.24가 있었다.

## 기존 구현: 일부 있음

- Naver 본문 읽기의 쉼표 selector는 우선순위가 아니라 DOM 순서였다. 바깥 ArticleContentBox가 안쪽 article_viewer보다 먼저 선택돼 작성자 77L 주스·댓글까지 근거에 들어왔다. d61966c8의 최초 읽기, d8ab0f9e의 구형/신형 DOM 지원, bcf7015e의 제목 보호 목적을 유지한다. 기존 benefit-search-utils로 인라인 읽기를 이동해 준비 대기와 추출이 같은 본문 소유자를 사용한다. 새 reader는 별도 수집기가 아니다.
- 4913535f의 `selectCandidateDates`는 명시 제목 날짜를 우선하고 지난 날짜를 제외하지만 AI fallback에 연결되지 않았다. Naver 원문 제목의 날짜를 화면용 cleanTitle로 지우지 않고 보존한다. 같은 판정을 일반 추출과 AI 호출 전에 적용하고, 결과 날짜가 제목 날짜 밖이면 저장하지 않고 기존 추출 실패/당일 재시도 경로에 남긴다. 날짜 없는 월간·포스터 안내는 기존 근거 검증을 유지한다. AI 확신도나 AI가 만든 전사로 원문의 명시 날짜를 덮을 수 없다.
- e3f76dc3의 엄격한 소셜 중복, ec0a7be1의 검증 후보 재개는 있었지만 일반 제목 유사도 fallback이 DJ 미상 후보를 다시 확정 중복으로 만들었다. 소셜의 확정 동일성 판정 후 일반 제목 fallback을 적용하지 않는다. 애매한 **공개** 같은 장소/날짜 충돌은 기존 `findSocialOccurrenceConflict`가 검토 대기로 보류하고 쓰기 잠금 안에서도 확인한다. 미등록 미검증 후보는 정상 후보의 공개 등록을 영구 차단하는 근거가 아니다.

## 보존 및 검증

새 DB·필드·큐·예약·상태는 없다. 같은 원문/날짜의 호환 내용, 같은 날짜/장소/확정 DJ, 공식 API 우선, 관리자 삭제·제외, 실제 등록 완료, 휴무, 생성 정규 일정 대체 및 수동 검토를 유지한다. 기존 후보 재개 함수는 검증 통과와 미완료 원후보를 여전히 요구한다. 기존 데이터를 제목·장소만 보고 합치거나 삭제하지 않는다.

회귀 범위는 실제 신형 DOM의 작성자/댓글 분리, 구형 본문, 빈 페이지/이미지만 있는 글, 지난 명시 날짜의 AI 호출 방지, AI 날짜 충돌·정상 결과·날짜 없는 안내, 잘못된 중복 후보 intake→공개 등록→동일 이벤트 재시도, 공개 충돌/잠금/수동 경로, 당일 실패→재시도→공개 확인→중단, 휴무·관리자 삭제다. 기존 검사 7파일 159개와 표준 수집 검사를 통과했다. 운영 원장 사본을 읽기 전용으로 대입해 정상 후보의 validation 통과·재개 가능·다른 후보 중복 없음·공개 충돌 없음을 확인했다.

관계없는 UI/통계 배포, 전체 SNS 탐색, 반복 빈도 변경, 원장 일괄 삭제는 제외한다. 인식할 수 없는 본문이나 충돌한 근거는 미등록으로 남긴다. 이미지 인식이 항상 정확해졌다고 보장하지 않는다.

## 운영 반영과 복구

운영 코드의 기존 사본과 비교한 해당 변경만 적용하고 원본을 보관한다. Mini PC 두 모듈의 수정 전 내용은 로컬 수정 전 사본과 일치했다. 서버는 다른 미배포 목록 UI 변경이 있어 로컬 파일 전체를 덮어쓰지 않고 중복 판정 부분만 적용한다. 진행 파일·기존 후보는 초기화하지 않는다. 롤백은 보관한 모듈을 복원하고 해당 서비스만 재시작한다. 실행 및 실제 공개 결과는 ISSUE_LOG에 남긴다.


운영 재검증에서 드러난 장소 근거 판정의 중복 구현도 제거했다. c3250dac5의 동의어 인정 의도를 보존하되, AI와 서버의 별도 치환 대신 기존 venueNormalization.mjs 별칭을 문장 안의 제한된 구절에도 적용한다. 새 별칭 목록은 없다. 원문에 실제로 없는 인용·다른 장소·근거 누락은 계속 거부한다. 같은 파일의 독립 장소명 정규화 동작은 바꾸지 않는다.

최종 실제 결과: 9/24 해림 이벤트 513d8f90-2dd0-42eb-a368-527c7de18ed4 등록 및 공개 확인. 이후 같은 예약 조건 실행은 대상 0·브라우저 미기동이었다. 인접 9/26의 기존 쓴귤 표기는 본문/원본/저장 포스터 모두 테일임을 대조한 뒤 기존 이벤트 9b42fd60-9cd7-4cb3-a23f-ae1c0d9f6df2의 제목/설명만 정상 저장 경로·잠금·동시 변경 검사·백업 아래 정정했다. 원래 추출 원장은 감사 이력으로 보존하고 자동으로 여러 후보를 합치지 않았다.
