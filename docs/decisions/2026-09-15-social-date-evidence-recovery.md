# 날짜 근거 표기 호환 및 당일 재수집 간격

- 날짜: 2026-09-15
- 상태: 구현·테스트·서버 배포·오늘 경성홀 자동등록 복구 및 예약 활성화 완료.

## 재발 조사와 원래 보호 목적

경성홀 당일 미등록 → 08:00 및 12:30 재수집에서 공식 DdRBWKyS8C1 재독 → AI 반환 날짜 증거 검사에서 차단 → remainingSources 경성홀 유지·공개 기본 소셜 유지. 재수집 누락이 아니라 날짜 표기 비호환이었다. 실제 원본 포스터는 `2026 09 15 (화)`, 자동 대체 텍스트는 `20260915`를 포함한다. AI evidenceMentionsDate, native extractDates, 서버 evidenceExplicitlyContainsCandidateDate는 구분자/한글 날짜만 지원했다. fe2b77c6의 AI·서버 검사는 환각 날짜 방지, 이후 날짜 목록 처리는 다중 회차와 시각 오인식 방지가 목적이었다.

9/13의 aca1e9e0은 같은 날 기본 소셜 대조·기존 remainingSources 연결을 추가했지만 실패 직후 실행하지 않고 기존 5회 예약에서만 재시도했다. 또한 AI 재검증은 텍스트 날짜 힌트가 있어야 작동해 이미지에서만 추출한 회차의 근거 실패를 재판독하지 못했다.

## 기존 구현 판정 및 변경

- **일부 있음**: 세 계층의 기존 날짜 판정 함수를 확장한다. 명시 연도+월+일의 공백/붙여쓰기만 지원하고 긴 숫자/식별자 부분 일치, 연도 불일치, 시간만 있는 문자열은 제외한다. 원문과 증거 인용을 정규화한 가짜 문장으로 바꾸지 않는다. native 날짜 힌트는 실제 유효 달력 날짜만 허용한다.
- 기존 AI focused recovery를 재사용한다. 추출 세션이 있으나 근거 검증에 실패하면 숫자 날짜 힌트가 없어도 동일 전체 제한 시간 안에서 1회 재검증한다. 실패 사유를 피드백하되 소스 증거로 쓰지 않도록 지시한다. 재검증 결과에도 동일 정확 인용/날짜/장소/DJ/0.98 기준을 적용한다. 새 모델/AI API/큐는 없다.
- **예약/대기 구현 있음**: 같은 priority1/2 LaunchAgent에 보충 트리거만 추가한다. priority1은 08:00 및 09:30–20:30 매시, priority2는 09:00–21:00 매시다. 기존 전체 수집 시간대(1: 8/12/16/18/20시, 2: 9/13/17/19/21시)는 유지한다. INGESTION_NATIVE_FULL_SCAN_HOURS 설정으로 그 외 시간대에는 당일 미확정 소스만 대조·수집한다. 설정이 없는 수동/레거시 실행은 기존 전체 수집이다.
- 보충 실행은 다른 실패 소스의 대기 기록을 지우거나 전체 수집 완료 시각을 올리지 않는다. 대상 없음 또는 공개 API 조회 실패 때는 브라우저를 열지 않으며 기존 체크포인트를 변경하지 않는다. API 실패는 이슈로 표시한다. 새로운 상태 필드/DB 테이블/작업 소유자가 없고 동일 잠금으로 직렬화한다. priority3/4와 겹치는 예약은 기존 잠금 대기 정책을 따른다. 로그인/접근 실패 소스를 한 실행 안에서 재방문하지 않는다.
- 실제 보충/전체 구분은 실행 시작의 한국 시간대 hour를 사용한다(초기 실행 지연 허용). Mac 절전/전원 종료/긴 잠금 대기는 예약을 지연시킬 수 있어 1시간 내 성공 보장은 아니다. 관리자 삭제·휴무/override·등록 중복·미수집 기본 일정 보존은 유지한다.

## 검증/영향/롤백

- 실제 경성홀 공식 게시물을 새 브라우저로 읽고 1350px 원본 포스터를 시각 확인했다. 수정 코드로 원문 재입력: 9/15, 경성홀, DJ Deniz 후보 1개, 원본 공백 날짜 인용 그대로 AI 추출·독립 등록 판정 0.99 승인. 전체 정확도 99% 실측 의미는 아니다.
- 기존 AI/서버 등록 검사에 공백·줄바꿈·붙인 날짜, 잘못된 연도/긴 숫자/시각 제외, 날짜 힌트 없는 근거 재판독 성공/허위 근거 실패를 확장했다. 관련 검사 110개, 표준 검사 통과.
- 실제 native 흐름에서 외부 브라우저/API만 모의한 보충 실행 4경계 통과: 미확정 유지, 반영 후 대상만 해제, API 실패·대상 없음의 브라우저 미실행. 모든 경우 다른 잔여 소스와 마지막 전체 완료 시각 보존 확인. 날짜 힌트의 잘못된 달력 날짜 및 식별자 제외도 native 실행으로 확인했다.
- 서버 등록 검증도 같은 날짜 형식이 필요하므로 웹 서비스 배포에 포함한다. 다른 UI/운영 데이터 구조를 변경하지 않고 기존 배포 명령·빌드·health/version 검증을 사용한다. 무관한 UI 전체 테스트는 제외한다. 코드/두 plist를 이전 버전으로 되돌려 재로드하면 롤백 가능하며 데이터 마이그레이션은 없다.

## 운영 반영

- 661fbd6f를 origin/codex/same-day-date-recovery-20260915에 먼저 푸시한 뒤 기존 Cafe24 배포 명령 실행 완료. buildTime=1789443495777, __health ok, 서비스 active. 운영 AI 모듈/서버 등록 함수 파일 해시가 검증 작업본과 일치한다.
- 공식 원문과 원본 포스터로 검증된 후보 31da783022910786을 기존 postCandidate→수집 API→automatic 등록 API로 처리했다. 이벤트 23cbf65d-4af9-4c70-b7f9-d20f22277239, 2026-09-15, 경성홀, DJ Deniz. 직접 DB 수정이나 검증 우회 없이 자동등록 1건 성공.
- 공개 API에서 오늘 경성홀 실제 공지 1건 및 기본 미정 생성본 교체, 원본 링크 일치 확인. 로컬 저장 포스터 HTTP 200. 기존 삭제된 9/12·9/13 경성홀 소셜은 별도로 재생성하지 않았다.
- 실제 예약 실행 폴더에도 같은 변경을 반영했다. 카페 priority2 및 기존 12:30 실행 종료 후 priority1 재로드 완료. 두 LaunchAgent 각각 13개 시간 트리거와 전체 수집 시간대 설정을 확인했다. 진행 중이던 수집을 강제 종료하지 않았다.
- 후속 db1ec557은 보충 조회 실패 시 대기 큐가 비었더라도 성공 종료하지 않도록 Mac native 종료 상태를 보완했다. 실제 native 종료 흐름의 모의 503/빈 큐 검증에서 종료 상태 1·브라우저 0·기존 상태 보존 확인. 서버 API/AI 변경 없는 로컬 실행기 보완이라 웹 번들 재배포는 하지 않았으며 운영 서버 등록 검증은 661fbd6f 배포본과 동일하다.
