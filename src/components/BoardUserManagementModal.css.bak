/* BoardUserManagementModal.css - BoardUserManagementModal 컴포넌트 전용 스타일 */

/* ===== CSS 변수 정의 ===== */
:root {
  /* 색상 변수 */
  --boum-gray-300: #d1d5db;
  --boum-gray-400: #9ca3af;
  --boum-gray-600: #4b5563;
  --boum-gray-700: #374151;
  --boum-gray-750: #2d3748;
  --boum-gray-800: #1f2937;
  --boum-gray-900: #111827;
  
  --boum-blue-300: #93c5fd;
  --boum-blue-500: #3b82f6;
  --boum-blue-600: #2563eb;
  --boum-blue-700: #1d4ed8;
  --boum-blue-900: #1e3a8a;
  
  --boum-pink-300: #f9a8d4;
  --boum-pink-900: #831843;
  
  /* 간격 */
  --boum-spacing-2: 0.5rem;
  --boum-spacing-3: 0.75rem;
  --boum-spacing-4: 1rem;
  --boum-spacing-6: 1.5rem;
  --boum-spacing-8: 2rem;
  --boum-spacing-12: 3rem;
  
  /* Border Radius */
  --boum-radius-lg: 0.5rem;
  --boum-radius-full: 9999px;
  
  /* Z-index */
  --boum-z-1000000: 1000000;
  
  /* 트랜지션 */
  --boum-transition-colors: color 0.2s ease, background-color 0.2s ease;
}

/* ===== 모달 오버레이 ===== */
.boum-overlay {
  position: fixed;
  top: 0;
  right: 0;
  bottom: 0;
  left: 0;
  background-color: rgba(0, 0, 0, 0.5);
  display: flex;
  align-items: center;
  justify-content: center;
  padding: var(--boum-spacing-4);
  z-index: var(--boum-z-1000000);
}

/* ===== 모달 컨테이너 ===== */
.boum-container {
  background-color: var(--boum-gray-800);
  border-radius: var(--boum-radius-lg);
  max-width: 80rem;
  width: 100%;
  max-height: 90svh;
  position: relative;
  z-index: var(--boum-z-1000000);
  display: flex;
  flex-direction: column;
  overflow: hidden;
}

/* ===== 모달 헤더 ===== */
.boum-header {
  padding: var(--boum-spacing-4);
  border-bottom: 1px solid var(--boum-gray-700);
  flex-shrink: 0;
}

.boum-header-top {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: var(--boum-spacing-3);
}

.boum-title {
  font-size: 1.25rem;
  line-height: 1.75rem;
  font-weight: 700;
  color: white;
}

.boum-close-btn {
  color: var(--boum-gray-400);
  transition: var(--boum-transition-colors);
  background: none;
  border: none;
  cursor: pointer;
  padding: 0;
}

.boum-close-btn:hover {
  color: white;
}

.boum-close-icon {
  font-size: 1.5rem;
  line-height: 2rem;
}

/* ===== 검색 영역 ===== */
.boum-search-area {
  display: flex;
  align-items: center;
  gap: var(--boum-spacing-2);
}

.boum-search-wrapper {
  flex: 1;
  position: relative;
}

.boum-search-input {
  width: 100%;
  background-color: var(--boum-gray-700);
  color: white;
  border-radius: var(--boum-radius-lg);
  padding: var(--boum-spacing-2) var(--boum-spacing-3);
  padding-left: 2.5rem;
  border: none;
  outline: none;
}

.boum-search-input:focus {
  outline: none;
  box-shadow: 0 0 0 2px var(--boum-blue-500);
}

.boum-search-input::placeholder {
  color: var(--boum-gray-400);
}

.boum-search-icon {
  position: absolute;
  left: var(--boum-spacing-3);
  top: 50%;
  transform: translateY(-50%);
  color: var(--boum-gray-400);
}

.boum-refresh-btn {
  background-color: var(--boum-blue-600);
  color: white;
  padding: var(--boum-spacing-2) var(--boum-spacing-4);
  border-radius: var(--boum-radius-lg);
  transition: var(--boum-transition-colors);
  border: none;
  cursor: pointer;
}

.boum-refresh-btn:hover {
  background-color: var(--boum-blue-700);
}

/* ===== 통계 영역 ===== */
.boum-stats {
  margin-top: var(--boum-spacing-3);
  display: flex;
  align-items: center;
  gap: var(--boum-spacing-4);
  font-size: 0.875rem;
  line-height: 1.25rem;
  color: var(--boum-gray-400);
}

/* ===== 모달 컨텐츠 ===== */
.boum-content {
  flex: 1;
  overflow-y: auto;
  padding: var(--boum-spacing-4);
}

/* ===== 로딩 상태 ===== */
.boum-loading {
  text-align: center;
  padding: var(--boum-spacing-12) 0;
}

.boum-loading-icon {
  font-size: 1.875rem;
  line-height: 2.25rem;
  color: var(--boum-blue-500);
  animation: spin 1s linear infinite;
}

@keyframes spin {
  from {
    transform: rotate(0deg);
  }
  to {
    transform: rotate(360deg);
  }
}

.boum-loading-text {
  color: var(--boum-gray-400);
  margin-top: var(--boum-spacing-2);
}

/* ===== 빈 상태 ===== */
.boum-empty {
  text-align: center;
  padding: var(--boum-spacing-12) 0;
}

.boum-empty-icon {
  font-size: 3rem;
  line-height: 1;
  color: var(--boum-gray-600);
  margin-bottom: var(--boum-spacing-4);
}

.boum-empty-text {
  color: var(--boum-gray-400);
}

/* ===== 테이블 ===== */
.boum-table-wrapper {
  overflow-x: auto;
}

.boum-table {
  width: 100%;
}

.boum-table-head {
  border-bottom: 1px solid var(--boum-gray-700);
}

.boum-table-header {
  text-align: left;
  color: var(--boum-gray-300);
  font-weight: 500;
  padding: var(--boum-spacing-3);
}

.boum-table-row {
  border-bottom: 1px solid var(--boum-gray-700);
  transition: var(--boum-transition-colors);
}

.boum-table-row:hover {
  background-color: var(--boum-gray-750);
}

.boum-table-cell {
  padding: var(--boum-spacing-3);
}

/* ===== 테이블 - 닉네임 셀 ===== */
.boum-nickname-cell {
  display: flex;
  align-items: center;
  gap: var(--boum-spacing-2);
}

.boum-avatar {
  width: 2rem;
  height: 2rem;
  border-radius: var(--boum-radius-full);
  background-color: var(--boum-blue-600);
  display: flex;
  align-items: center;
  justify-content: center;
  color: white;
  font-weight: 700;
  font-size: 0.875rem;
  line-height: 1.25rem;
}

.boum-nickname {
  color: white;
  font-weight: 500;
}

/* ===== 테이블 - 본명/전화번호 셀 ===== */
.boum-text-gray {
  color: var(--boum-gray-300);
}

/* ===== 테이블 - 성별 셀 ===== */
.boum-gender-badge {
  padding: var(--boum-spacing-2) var(--boum-spacing-2);
  padding-top: var(--boum-spacing-1);
  padding-bottom: var(--boum-spacing-1);
  border-radius: var(--boum-radius-lg);
  font-size: 0.75rem;
  line-height: 1rem;
  font-weight: 500;
}

.boum-gender-male {
  background-color: rgba(30, 58, 138, 0.3);
  color: var(--boum-blue-300);
}

.boum-gender-female {
  background-color: rgba(131, 24, 67, 0.3);
  color: var(--boum-pink-300);
}

.boum-gender-other {
  background-color: var(--boum-gray-700);
  color: var(--boum-gray-300);
}

/* ===== 테이블 - 가입일 셀 ===== */
.boum-date-text {
  color: var(--boum-gray-400);
  font-size: 0.875rem;
  line-height: 1.25rem;
}

/* ===== 모달 푸터 ===== */
.boum-footer {
  padding: var(--boum-spacing-4);
  border-top: 1px solid var(--boum-gray-700);
  flex-shrink: 0;
}

.boum-footer-btn {
  width: 100%;
  background-color: var(--boum-gray-700);
  color: white;
  padding: var(--boum-spacing-2);
  border-radius: var(--boum-radius-lg);
  font-weight: 500;
  transition: var(--boum-transition-colors);
  border: none;
  cursor: pointer;
}

.boum-footer-btn:hover {
  background-color: var(--boum-gray-600);
}
