/* PostDetailModal.css - PostDetailModal 컴포넌트 전용 스타일 */

/* ===== CSS 변수 정의 ===== */
:root {
  /* 색상 변수 */
  --pdm-gray-200: #e5e7eb;
  --pdm-gray-400: #9ca3af;
  --pdm-gray-600: #4b5563;
  --pdm-gray-700: #374151;
  --pdm-gray-800: #1f2937;
  
  --pdm-blue-600: #2563eb;
  --pdm-blue-700: #1d4ed8;
  
  --pdm-red-600: #dc2626;
  --pdm-red-700: #b91c1c;
  
  /* 간격 */
  --pdm-spacing-1: 0.25rem;
  --pdm-spacing-2: 0.5rem;
  --pdm-spacing-4: 1rem;
  --pdm-spacing-6: 1.5rem;
  
  /* Border Radius */
  --pdm-radius-lg: 0.5rem;
  
  /* Z-index */
  --pdm-z-modal: 999999;
  
  /* 트랜지션 */
  --pdm-transition-colors: color 0.2s ease, background-color 0.2s ease;
}

/* ===== 모달 오버레이 ===== */
.pdm-modal-overlay {
  position: fixed;
  top: 0;
  right: 0;
  bottom: 0;
  left: 0;
  background-color: rgba(0, 0, 0, 0.5);
  display: flex;
  align-items: center;
  justify-content: center;
  padding: var(--pdm-spacing-4);
  z-index: var(--pdm-z-modal);
}

/* ===== 모달 컨테이너 ===== */
.pdm-modal-container {
  background-color: var(--pdm-gray-800);
  border-radius: var(--pdm-radius-lg);
  max-width: 42rem;
  width: 100%;
  max-height: 90svh;
  position: relative;
  z-index: var(--pdm-z-modal);
  display: flex;
  flex-direction: column;
  overflow: hidden;
}

/* ===== 모달 헤더 ===== */
.pdm-modal-header {
  padding: var(--pdm-spacing-4);
  border-bottom: 1px solid var(--pdm-gray-700);
  flex-shrink: 0;
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.pdm-header-content {
  flex: 1;
}

.pdm-modal-title {
  font-size: 1.25rem;
  line-height: 1.75rem;
  font-weight: 700;
  color: white;
  margin-bottom: var(--pdm-spacing-2);
}

.pdm-meta-info {
  display: flex;
  align-items: center;
  gap: var(--pdm-spacing-4);
  font-size: 0.875rem;
  line-height: 1.25rem;
  color: var(--pdm-gray-400);
}

.pdm-meta-item {
  display: flex;
  align-items: center;
  gap: var(--pdm-spacing-1);
}

.pdm-close-btn {
  color: var(--pdm-gray-400);
  transition: var(--pdm-transition-colors);
  margin-left: var(--pdm-spacing-4);
  background: none;
  border: none;
  cursor: pointer;
  padding: 0;
}

.pdm-close-btn:hover {
  color: white;
}

.pdm-close-icon {
  font-size: 1.5rem;
  line-height: 2rem;
}

/* ===== 모달 본문 ===== */
.pdm-modal-body {
  flex: 1;
  overflow-y: auto;
  padding: var(--pdm-spacing-4) var(--pdm-spacing-4) var(--pdm-spacing-6);
}

.pdm-content {
  color: var(--pdm-gray-200);
  white-space: pre-wrap;
  word-break: break-word;
}

/* ===== 모달 푸터 ===== */
.pdm-modal-footer {
  padding: var(--pdm-spacing-4);
  border-top: 1px solid var(--pdm-gray-700);
  flex-shrink: 0;
}

.pdm-footer-actions {
  display: flex;
  gap: var(--pdm-spacing-2);
}

.pdm-btn {
  flex: 1;
  padding: 0.5rem;
  border-radius: var(--pdm-radius-lg);
  font-weight: 500;
  transition: var(--pdm-transition-colors);
  cursor: pointer;
  border: none;
  color: white;
}

.pdm-btn-close {
  background-color: var(--pdm-gray-700);
}

.pdm-btn-close:hover {
  background-color: var(--pdm-gray-600);
}

.pdm-btn-edit {
  background-color: var(--pdm-blue-600);
}

.pdm-btn-edit:hover {
  background-color: var(--pdm-blue-700);
}

.pdm-btn-delete {
  background-color: var(--pdm-red-600);
}

.pdm-btn-delete:hover {
  background-color: var(--pdm-red-700);
}

.pdm-btn-icon {
  margin-right: var(--pdm-spacing-1);
}
